<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WeatherApp"
        android:usesCleartextTraffic="true">

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.SearchActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.FavoriteCitiesActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.MapActivity"
            android:exported="false"
            android:theme="@style/Theme.WeatherApp.NoActionBar" />
        <activity
            android:name=".ui.activities.ChartsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.IOSWeatherActivity"
            android:exported="true"
            android:theme="@style/Theme.WeatherApp.NoActionBar" />
        <activity
            android:name=".ui.activities.OutfitSuggestionActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.ActivitySuggestionsActivity"
            android:exported="false"
            android:theme="@style/Theme.WeatherApp" />
        <activity
            android:name=".ui.activities.WeatherMapsActivity"
            android:exported="false"
            android:theme="@style/Theme.WeatherApp" />
        <activity
            android:name=".ui.activities.OSMWeatherMapActivity"
            android:exported="false"
            android:theme="@style/Theme.WeatherApp.NoActionBar" />
        <activity
            android:name=".ui.activities.VoiceWeatherActivity"
            android:exported="false"
            android:theme="@style/Theme.WeatherApp.NoActionBar"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.activities.WeatherChatBotActivity"
            android:exported="false"
            android:theme="@style/Theme.WeatherApp.NoActionBar"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ui.WeatherAlarmActivity"
            android:exported="false"
            android:theme="@style/Theme.WeatherApp.NoActionBar"
            android:screenOrientation="portrait" />

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBXa_YOUR_API_KEY_HERE" />

        <!-- Notification Receiver -->
        <receiver
            android:name=".notification.NotificationReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.weatherapp.ACTION_REFRESH_WEATHER" />
            </intent-filter>
        </receiver>
        
        <!-- Weather Alarm Receiver -->
        <receiver
            android:name=".notification.WeatherAlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Weather Widget -->
        <receiver
            android:name=".widget.WeatherWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/weather_widget_info" />
        </receiver>
    </application>

</manifest>