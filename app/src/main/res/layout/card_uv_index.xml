<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/ios_card_background"
    android:padding="16dp"
    android:clickable="true"
    android:focusable="true">

    <!-- Icon and Title Row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="12dp">

        <TextView
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:text="☀"
            android:textSize="14sp"
            android:gravity="center"
            android:layout_marginEnd="6dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="UV INDEX"
            android:textColor="@color/text_secondary"
            android:textSize="11sp"
            android:fontFamily="@font/poppins"
            tools:ignore="HardcodedText" />
    </LinearLayout>

    <!-- UV Value -->
    <TextView
        android:id="@+id/tvDetailValue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="4"
        android:textColor="@color/text_primary"
        android:textSize="36sp"
        android:textStyle="bold"
        android:fontFamily="@font/poppins_bold"
        android:layout_marginBottom="4dp"
        tools:ignore="HardcodedText" />

    <!-- UV Level -->
    <TextView
        android:id="@+id/tvUvLevel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Moderate"
        android:textColor="@color/text_primary"
        android:textSize="13sp"
        android:fontFamily="@font/poppins"
        android:layout_marginBottom="16dp"
        tools:ignore="HardcodedText" />

    <!-- iOS UV Gradient Bar -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="6dp"
        android:background="@drawable/ios_uv_progress_bg">
        
        <View
            android:id="@+id/uvGradientBar"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:background="@drawable/ios_uv_gradient" />
        
    </FrameLayout>

    <!-- UV Scale Labels -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="8dp">
        
        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="0"
            android:textColor="@color/text_secondary"
            android:textSize="11sp"
            android:gravity="start"
            tools:ignore="HardcodedText" />
        
        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="11+"
            android:textColor="@color/text_secondary"
            android:textSize="11sp"
            android:gravity="end"
            tools:ignore="HardcodedText" />
        
    </LinearLayout>

    <!-- Description -->
    <TextView
        android:id="@+id/tvDetailDescription"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Use sunscreen for extended outdoor activity."
        android:textColor="@color/text_secondary"
        android:textSize="11sp"
        android:fontFamily="@font/poppins"
        android:maxLines="2"
        tools:ignore="HardcodedText" />
</LinearLayout>
