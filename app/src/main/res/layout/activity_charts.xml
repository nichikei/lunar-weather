<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/gradient_background">

        <!-- Header -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="16dp"
            android:gravity="center_vertical">

            <ImageButton
                android:id="@+id/btnBack"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_arrow_back"
                app:tint="@color/text_primary"
                android:contentDescription="@string/settings" />

            <TextView
                android:id="@+id/tvChartTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/weather_statistics"
                android:textColor="@color/text_primary"
                android:textSize="20sp"
                android:fontFamily="@font/poppins_semibold"
                android:layout_marginStart="16dp" />

        </LinearLayout>

        <!-- Charts Container -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Temperature Chart -->
            <include
                android:id="@+id/card_temperature_chart"
                layout="@layout/card_temperature_chart" />

            <!-- Weather Statistics Chart -->
            <include
                android:id="@+id/card_weather_stats_chart"
                layout="@layout/card_weather_stats_chart" />

            <!-- Rain Probability Chart -->
            <include
                android:id="@+id/card_rain_probability_chart"
                layout="@layout/card_rain_probability_chart" />

            <!-- Wind Speed Chart -->
            <include
                android:id="@+id/card_wind_speed_chart"
                layout="@layout/card_wind_speed_chart" />

            <!-- Humidity Chart -->
            <include
                android:id="@+id/card_humidity_chart"
                layout="@layout/card_humidity_chart" />

        </LinearLayout>

    </LinearLayout>

</ScrollView>
